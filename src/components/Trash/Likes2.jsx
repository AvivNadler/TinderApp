/* import React from 'react'
import UserDetails from '../UserDetails'

const TinderLikes=(params)=> {
    return (
      <div className='container' style={{height:'70vh'}}>
      <div className='row'>
      
     {/* {parseInt(params.myLikes.length)>=parseInt(params.index)+1?<div><UserComponent item={params.myLikes[params.index]}></UserComponent> <div className='col-12' style={{position:'fixed',left:'0',bottom:'1vh'}}>
          <TinderButtons addLiked={params.addLiked} item={params.myLikes[params.index]} num={parseInt(params.index)+1}></TinderButtons>
      </div></div>:<div style={{paddingTop:'100px', width: "100%", objectFit: "contain "}} ><Spinner style={{height:'20vh',width:'20vh' }} animation="grow" variant="info"></Spinner><h5>Looking for new users...</h5></div>}
       *//* }
       {params.myLikes.map(item=>{return(
         <div className='col-4' >
           <UserDetails item={item}></UserDetails>
         </div>
       )})}
     

      </div>

  </div>
    )
}
export default TinderLikes; */

  